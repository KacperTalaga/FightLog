/* Widok BAZA WIEDZY — statyczne sekcje o progresji, badaniach i regeneracji. */

export function mountKnowledge(container) {
    container.innerHTML = `
    <h1 class="view__title">Baza wiedzy</h1>
    <div class="empty">
        <strong>Sekcje w przygotowaniu</strong>
        System progresji, kluczowe badania, odżywianie i hierarchia regeneracji —
        wszystko offline, z wyszukiwarką.
    </div>`;
}
